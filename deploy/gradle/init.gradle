// Keep init script non-invasive.
// Repository resolution is defined in settings.gradle.kts via
// dependencyResolutionManagement + pluginManagement.
// This avoids conflicts with RepositoriesMode.FAIL_ON_PROJECT_REPOS.
