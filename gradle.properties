# Gradle 多镜像源配置
# 使用阿里云镜像作为主源，官方源作为备用源

# 启用依赖项验证（可选）
org.gradle.dependency.verification=off

# 增加网络超时时间（秒）
org.gradle.internal.http.timeout=60

# 启用并行构建（可选）
org.gradle.parallel=true

# 启用离线模式的部分支持
# org.gradle.offline=false
